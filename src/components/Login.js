import React, { Component } from 'react';
import Subtitle from './Subtitle';

class Login extends Component {
    render() {
        return (
            <div>
                <Subtitle value="Login"/>
            </div>
        );
    }
}

export default Login;
